# 📋 Задача 5.1: Обновление статуса и отправка видео

**User Story:** Как разработчик, я хочу, чтобы после успешного монтажа видео воркер обновлял статус заказа в БД на `completed` и отправлял готовый видеофайл пользователю в Telegram-чат.

## Требования

1.  **Обновление статуса:**
    -   В начале обработки задачи установить статус `VideoJob` в `PROCESSING`.
    -   После успешной отправки видео установить статус `COMPLETED`.
    -   В случае любой ошибки на этапе обработки установить статус `FAILED`.
2.  **Отправка видео:**
    -   Использовать экземпляр `Bot API` для отправки видео пользователю.
    -   Видео должно отправляться с сопроводительным текстом (например, "Ваше персональное новогоднее поздравление! 🎉").
3.  **Обработка ошибок:**
    -   Обернуть всю логику обработки в `try-catch`.
    -   В случае ошибки (например, пользователь заблокировал бота), она должна быть залогирована, а статус задачи — обновлен на `FAILED`.
4.  **Уведомление:**
    -   После успешной отправки видео отправить дополнительное сообщение с подтверждением и кнопкой для заказа нового видео.

## Сложность
🟡 СТАНДАРТНАЯ
