# 📋 Задача 5.2: Кэширование и дедупликация задач на генерацию видео

**User Story:** Как пользователь, если я запрашиваю видео для имени, которое уже было недавно сгенерировано или находится в процессе генерации, я хочу получить результат быстрее и не создавать лишнюю нагрузку на систему.

## Требования

1.  **Кэширование готовых видео:**
    *   При поступлении нового запроса на генерацию видео для имени `X` система должна сначала проверить, существует ли уже готовое видео для этого имени.
    *   Для этого потребуется новая модель в `prisma/schema.prisma`, например `GeneratedVideo`, которая будет хранить связь между **именем ребенка**, **полом** (для будущих доработок) и **Telegram `file_id`** сгенерированного видео.
    *   Если готовое видео найдено, оно должно быть немедленно отправлено пользователю без запуска процесса генерации.

2.  **Дедупликация активных задач:**
    *   Если готовое видео не найдено, система должна проверить, есть ли уже активная задача (в статусе `PENDING` или `PROCESSING`) на генерацию для этого имени.
    *   Если такая задача существует, новый запрос не должен создавать дублирующую задачу в BullMQ. Вместо этого, `userId` нового заказчика должен быть "подписан" на результат существующей задачи.
    *   После того как существующая задача будет выполнена, сгенерированное видео должно быть отправлено всем "подписавшимся" пользователям.

3.  **Создание новой задачи:**
    *   Только если не найдено ни готового видео, ни активной задачи, должна создаваться новая задача на генерацию.

## Проблемы и риски

-   **Гонка состояний (Race Condition):** Два пользователя могут одновременно запросить одно и то же имя. Необходимо использовать блокировки на уровне БД (например, `SELECT ... FOR UPDATE` в PostgreSQL или транзакции с определенным уровнем изоляции) для атомарного создания задачи.
-   **Сложность логики подписки:** Механизм хранения "подписчиков" на активную задачу требует тщательного проектирования, чтобы гарантировать доставку всем ожидающим пользователям.

## Сложность
🔴 КОМПЛЕКСНАЯ (требует изменения схемы данных, обработки гонки состояний и сложной логики подписок).
