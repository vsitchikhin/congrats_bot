# 📋 Задача 4.1: Генерация аудио-обращения (TTS) с помощью ElevenLabs

**User Story:** Как разработчик, я хочу иметь сервис, который генерирует аудиофайл с именем ребенка, используя API `elevenlabs.io`, чтобы в дальнейшем наложить его на видео.

## Требования

1.  **API:** Интегрироваться с API `elevenlabs.io` для синтеза речи.
2.  **Входные данные:** Сервис должен принимать на вход строку с именем ребенка (например, "Ваня").
3.  **Логика:** К полученному имени ребенка необходимо добавить восклицательный знак "!" в конце (например, "Ваня" -> "Ваня!"), и уже эту строку отправлять на генерацию в API.
4.  **Выходные данные:** Сервис должен генерировать и сохранять во временную директорию аудиофайл (например, в формате `.mp3`), содержащий озвученное имя.
5.  **Конфигурация:**
    *   API-ключ для ElevenLabs должен задаваться через переменные окружения (`ELEVENLABS_API_KEY`).
    *   ID голоса (Voice ID) для использования также должен быть вынесен в конфигурацию (`ELEVENLABS_VOICE_ID`).

## Сложность
🟠 MEDIUM (требуется интеграция с внешним API и обработка бинарных данных).
