# 📋 Задача 4.2: Обработка и монтаж видео (`ffmpeg`)

**User Story:** Как разработчик, я хочу иметь сервис для монтажа видео, который накладывает сгенерированный аудиофайл с именем ребенка на исходное видеопоздравление, сохраняя при этом исходное видео неизменным.

## Требования

1.  **Исходное видео:**
    *   Должен быть определен четкий и конфигурируемый путь к исходному видеофайлу-шаблону (например, через переменную окружения `SOURCE_VIDEO_PATH` или в директории типа `./assets`).
    *   Исходный видеофайл должен оставаться неизменным после операции монтажа.
2.  **Интеграция аудио:**
    *   Сервис должен принимать путь к сгенерированному аудиофайлу (имя ребенка).
    *   Аудиофайл должен быть вставлен в исходное видео, начиная с определенного, конфигурируемого таймкода (например, `AUDIO_INSERT_TIMECODE`).
3.  **Инструмент:** Использовать `ffmpeg` для выполнения операции монтажа. `ffmpeg` должен быть доступен в окружении.
4.  **Выходные данные:**
    *   Результатом работы сервиса должен быть новый видеофайл с наложенным аудио.
    *   Этот новый файл должен быть сохранен во временную директорию, готовую к отправке пользователю.
    *   Имя выходного файла должно быть уникальным, чтобы избежать конфликтов.

## Сложность
🔴 COMPLEX (интеграция с `ffmpeg`, работа с видеофайлами, возможные проблемы с производительностью).
